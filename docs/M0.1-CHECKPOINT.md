# M0.1 checkpoint

- Scope remains pure speaker diarization; no ASR dependency.
- Portable CPU correctness is green:
  - `ubuntu-24.04`, `windows-2022`, `macos-14` unit tests pass;
  - ASan/UBSan passes on Ubuntu;
  - source-hygiene passes.
- Kaggle P100 result is also valuable but negative:
  - GPU is `Tesla P100-PCIE-16GB`, `sm_60`, 16 GiB, driver `580.159.04`;
  - default `torch==2.10.0+cu128` sees CUDA but has no `sm_60` kernel;
  - first CUDA tensor op fails `cudaErrorNoKernelImageForDevice`.
- Immediate next test:
  1. reinstall `torch==2.10.0` from the `cu126` wheel index;
  2. compile/run the new `cuda_smoke.cu` with CUDA 12.x and `-arch=sm_60`;
  3. only then introduce a pinned native pure-diarization benchmark.
